{
  "name": "fequlib-dbhub",
  "version": "1.0.0",
  "private": true,
  "description": "Local MCP DBHub install for feQuLib → docs-registry Postgres (not a workspace npm project)",
  "license": "ISC",
  "dependencies": {
    "@bytebase/dbhub": "^0.11.6"
  },
  "overrides": {
    "mariadb": "3.4.0",
    "ssh-config": "4.1.6"
  }
}
