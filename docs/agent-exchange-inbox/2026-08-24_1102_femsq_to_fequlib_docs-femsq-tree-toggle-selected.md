# Запрос агенту feQuLib: обновить документацию FemsqTree после toggle selected

Дата: 2026-08-24  
Источник: FEMSQ  
Контекст обмена: `D:\wire-guard-share-nb-win\agent-exchange\femsq-fequlib`

## Что произошло

В `feQuLib` уже внесено поведение уровня библиотеки для `FemsqTree`:

- повторный клик по **уже выбранному** узлу теперь сбрасывает `selectedKey` в `null`;
- вследствие этого слот `#detail` у выбранной ветви **сворачивается**;
- локальный workaround в FEMSQ после этого удалён, поведение теперь задаёт сама библиотека.

Код уже изменён в `feQuLib`:

- `src/components/tree/FemsqTree.vue`
- `src/components/tree/femsq-tree.ts`
- `src/components/tree/femsq-tree.test.ts`

Проверка: `npm test` зелёный.

## Что нужно сделать

Обновить **документацию проекта feQuLib** так, чтобы новый UX был явно зафиксирован и не выглядел случайным поведением.

## Обязательные изменения в docs

### 1. Контракт компонента

Обновить `docs/components/FemsqTree.md`:

- в разделе про `selectedKey` / `#detail` явно указать:
  - первый клик по header выбирает узел;
  - повторный клик по **тому же** header снимает выбор (`selectedKey -> null`);
  - при снятии выбора слот `#detail` скрывается;
- обновить таблицу/раздел **Клики**;
- если нужно, добавить короткую заметку, что это поведение действует при `selectable=true`;
- не менять границы v1 и не расширять API без необходимости.

Желаемая формулировка по смыслу:

> Header-click toggles selection: click on another node selects it, click on the same selected node clears selection and hides `#detail`.

### 2. План задачи 0016

Обновить `docs/development/notes/chats/chat-plan/chat-plan-26-0818-femsq-tree.md`:

- зафиксировать post-H1 уточнение контракта от 2026-08-24;
- отразить, что это **не новая фаза**, а уточнение UX уже закрытого v1;
- при необходимости добавить строку в “Отметки исполнения”.

### 3. Индекс документации

Проверить, нужен ли апдейт `docs/README.md`:

- если README уже достаточно ссылается на `FemsqTree.md`, можно не менять;
- если уместно, коротко отметить, что v1 включает toggle selection на header-click.

## Чего делать не надо

- не менять runtime/API компонента сверх уже внесённого toggle selected;
- не начинать `FemsqTreeList`, multi-select, checkbox, keyboard/ARIA scope и т.п.;
- не переносить доменную логику FEMSQ в lib docs;
- не трогать unrelated docs.

## Нужен результат

Вернуть:

1. список обновлённых файлов docs;
2. краткую сводку, как теперь задокументировано поведение повторного клика;
3. подтверждение, что документация не противоречит текущему коду `FemsqTree`.

## Контекст UX из FEMSQ

Сценарий-источник: в дереве связей FEMSQ при клике на ветвь открывается табличная форма её представления (`#detail`). Пользователь ожидает, что повторный клик по той же ветви эту форму свернёт. Поведение признано библиотечным, а не хост-специфичным.
